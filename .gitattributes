# De werkboom staat overal op LF, ongeacht iemands core.autocrlf. Zonder dit bestand
# schrijft een verse kloon op Windows CRLF, en dan meet een test die een letterlijk
# regeleinde zoekt de checkout in plaats van de inhoud. De generator in check_tools.py
# schrijft altijd LF; zo blijven generator, CI en elke kloon gelijk.
* text=auto eol=lf
