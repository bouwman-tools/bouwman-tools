{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://bouwman.tools/tools.schema.json",
  "title": "Toolportefeuille bouwman.tools",
  "description": "Enige bron van waarheid voor de toolportefeuille. portal.html, beheer.html, access-beheer-worker.js en TOOLS.md horen hieruit te lezen of hieruit te worden gegenereerd.",
  "type": "object",
  "required": ["bijgewerkt", "categorievolgorde", "tools"],
  "properties": {
    "$schema": { "type": "string" },
    "toelichting": { "type": "string" },
    "bijgewerkt": { "$ref": "#/definitions/datum" },
    "statussen": { "type": "object" },
    "afscherming": { "type": "object" },
    "eigenaarschap": { "type": "object" },
    "categorievolgorde": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Volgorde waarin de categorieen in portal.html en beheer.html verschijnen. Een lijst voor beide paginas; elke categorie die in tools voorkomt hoort hier te staan, anders komt zij achteraan."
    },
    "portaalworkers": {
      "type": "array",
      "description": "Workers waar het portaal zelf van afhangt en die dus bij geen enkele tool horen. Zonder deze lijst zou de dagelijkse controle access-beheer als ongebruikt aanmerken, terwijl juist die worker het toegangsbeheer draait.",
      "items": {
        "type": "object",
        "required": ["naam", "waarvoor"],
        "additionalProperties": false,
        "properties": {
          "naam": { "$ref": "#/definitions/workernaam" },
          "waarvoor": { "type": "string", "minLength": 1 }
        }
      }
    },
    "tools": {
      "type": "array",
      "items": { "$ref": "#/definitions/tool" }
    },
    "vervallen": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["bestand", "reden"],
        "properties": {
          "bestand": { "type": "string" },
          "reden": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false,
  "definitions": {
    "datum": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "JJJJ-MM-DD"
    },
    "workernaam": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "Naam van een Cloudflare Worker zoals die op het account staat, niet de workers.dev-URL. Een adres bewijst niets: een worker met een uitgezette workers.dev-route antwoordt daar ook met 404. Het bewijs is of de naam in de scriptlijst van het account voorkomt."
    },
    "tool": {
      "type": "object",
      "required": [
        "id", "naam", "beschrijving", "categorie", "status", "repo",
        "in_portal", "in_beheer",
        "eigenaar", "beoordelingsritme", "laatst_beoordeeld",
        "jaarwaarden", "jaarwaarden_gecontroleerd", "uitgangen"
      ],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "naam": { "type": "string" },
        "beschrijving": {
          "type": "string",
          "minLength": 1,
          "description": "Korte omschrijving van wat de tool doet, zoals die in het portaal op de toolkaart staat. Enige plek waar deze tekst wordt vastgelegd."
        },
        "bestand": {
          "type": "string",
          "pattern": "[.]html$",
          "description": "Bestandsnaam in deze repo. Verplicht tenzij extern true is."
        },
        "url": {
          "type": "string",
          "description": "Externe locatie; alleen bij extern true."
        },
        "bestand_in_repo": {
          "type": "boolean",
          "description": "Standaard true. False wanneer het bestand bewust niet in deze repo staat omdat een eigen Worker de pagina op bouwman.tools serveert; de git-historie hier is publiek. De controle verwacht het bestand dan niet in de repo."
        },
        "extern": {
          "type": "boolean",
          "description": "Draait buiten deze repo, bijvoorbeeld op Streamlit of een eigen subdomein."
        },
        "categorie": { "type": "string" },
        "icon": {
          "type": "string",
          "minLength": 1,
          "maxLength": 8,
          "description": "Emoji op de toolkaart in het portaal. Verplicht zodra in_portal true is, want portal.html bouwt de kaarten hieruit op."
        },
        "tags": {
          "type": "array",
          "description": "Labels op de toolkaart in het portaal. De beta-tag hoort hier niet in: die leidt portal.html af uit status, zodat de status niet op twee plekken wordt bijgehouden.",
          "items": {
            "type": "object",
            "required": ["label"],
            "additionalProperties": false,
            "properties": {
              "label": { "type": "string", "minLength": 1 },
              "soort": {
                "enum": ["ai", "privacy-local", "privacy-ai"],
                "description": "Kleurmerk op het label; komt overeen met de tool-tag-klassen in portal.html. Weglaten voor een gewoon onderwerpslabel."
              }
            }
          }
        },
        "status": { "enum": ["live", "beta", "verborgen", "concept"] },
        "status_reden": { "type": "string" },
        "repo": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$",
          "description": "Bronrepository als organisatie/repo."
        },
        "workers": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/definitions/workernaam" },
          "description": "Cloudflare Workers waar deze tool van afhangt. De dagelijkse controle in access-beheer-worker.js legt deze namen naast de scriptlijst van het account en meldt wat ontbreekt. Weglaten als de tool geen Worker gebruikt. Aanleiding: op 04-09-2026 was kvk-proxy van het account verdwenen terwijl kvk-zoeker.html op live stond en in het portaal, en niemand merkte het."
        },
        "uc": { "type": "string" },
        "access_app_id": {
          "type": ["string", "null"],
          "pattern": "^[0-9a-f-]{36}$",
          "description": "Cloudflare Access-app. Zonder id is het bestand voor iedereen met de URL bereikbaar."
        },
        "in_portal": { "type": "boolean" },
        "in_beheer": { "type": "boolean" },
        "eigenaar": {
          "type": "string",
          "description": "Inhoudelijk verantwoordelijke die wijzigingen beoordeelt en accordeert. 'tbd' zolang dat niet is afgesproken."
        },
        "eigenaar_contact": {
          "type": "string",
          "description": "Optioneel: waar de vrijgavenotitie naartoe gaat."
        },
        "beoordelingsritme": { "enum": ["belastingplan", "jaarlijks", "geen"] },
        "laatst_beoordeeld": {
          "oneOf": [{ "$ref": "#/definitions/datum" }, { "type": "null" }],
          "description": "Datum waarop de eigenaar de tool inhoudelijk heeft geaccordeerd."
        },
        "jaarwaarden": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Namen van de jaargebonden constanten of blokken in de bronrepo. Leeg als de tool er geen heeft."
        },
        "jaarwaarden_gecontroleerd": {
          "oneOf": [{ "$ref": "#/definitions/datum" }, { "type": "null" }],
          "description": "Datum waarop de jaargebonden waarden tegen de bron zijn geverifieerd."
        },
        "opmerking": {
          "type": "string",
          "description": "Vrije toelichting bij een uitzondering op de gebruikelijke route, bijvoorbeeld een tool die niet via de sync wordt gepubliceerd. Alleen invullen als de afwijking anders onverklaarbaar is."
        },
        "uitgangen": {
          "type": "object",
          "description": "Wat een gebruiker uit de tool mee kan nemen. Gemeten 04-09-2026. dossierstuk: een afdruk die als dossierstuk is ontworpen, met de invoer erop en een noprint-klasse op knoppen en hulp; alleen een @media print-blok met een afdrukknop telt niet, want dan komt de invoer niet mee en is de uitkomst niet reproduceerbaar. excel: een export naar .xlsx. dossierbestand: opslaan naar en openen vanuit een bestand, zodat de casus in de klantmap kan staan en later heropend kan worden; localStorage telt niet, want dat zit vast aan een browser. false bij dossierstuk of excel is een openstaand punt; false bij dossierbestand kan een geldige keuze zijn bij weinig invoervelden. Geen enkele tool droeg op 04-09-2026 een versiestempel op de afdruk.",
          "required": ["dossierstuk", "excel", "dossierbestand"],
          "additionalProperties": false,
          "properties": {
            "dossierstuk": { "type": "boolean" },
            "excel": { "type": "boolean" },
            "dossierbestand": { "type": "boolean" }
          }
        }
      },
      "additionalProperties": false,
      "allOf": [
        {
          "if": { "properties": { "extern": { "const": true } }, "required": ["extern"] },
          "then": { "required": ["url"] },
          "else": { "required": ["bestand"] }
        },
        {
          "if": { "properties": { "in_portal": { "const": true } }, "required": ["in_portal"] },
          "then": { "required": ["icon"] }
        },
        {
          "if": {
            "properties": { "jaarwaarden": { "minItems": 1 } },
            "required": ["jaarwaarden"]
          },
          "then": {
            "properties": { "beoordelingsritme": { "const": "belastingplan" } }
          }
        }
      ]
    }
  }
}
